Tiny inline star meter for task cards and rows. Gold filled, line-gray empty.

```jsx
<StarPips max={3} />           // shows a task's max value
<StarPips max={5} filled={2} />// shows progress
```

Read-only and decorative; for awarding stars use `StarRating`.
